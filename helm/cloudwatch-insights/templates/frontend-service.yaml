apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloudwatch-insights.frontend.fullname" . }}
  labels:
    {{- include "cloudwatch-insights.frontend.selectorLabels" . | nindent 4 }}
spec:
  type: {{ .Values.frontend.service.type }}
  ports:
    - port: {{ .Values.frontend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cloudwatch-insights.frontend.selectorLabels" . | nindent 4 }}
