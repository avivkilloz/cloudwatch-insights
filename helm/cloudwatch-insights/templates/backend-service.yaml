apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloudwatch-insights.backend.fullname" . }}
  labels:
    {{- include "cloudwatch-insights.backend.selectorLabels" . | nindent 4 }}
spec:
  type: {{ .Values.backend.service.type }}
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cloudwatch-insights.backend.selectorLabels" . | nindent 4 }}
